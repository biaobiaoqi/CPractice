#include <stdio.h>
int ch;
int main()
{
	//freopen("G:\in.txt", "r", stdin);
	while(scanf("%c", &ch)!= EOF){
		if (ch == ' '){
			printf("%%20");
		}else {
			printf("%c", ch);
		}
	}
	//fclose(stdin);
	return 0;
}
